# Production dependencies for the Vercel Python function (api/index.py).
#
# EmberFlow is pinned to an exact commit so deployments are reproducible.
# Once EmberFlow is published to PyPI, replace the git pin with:
#   emberflow==0.1.0
#
# For LOCAL development do NOT use this file — `npm run dev:api` runs uvicorn
# via uv with an editable install of the sibling ../emberflow checkout.
emberflow @ git+https://github.com/alanx1234/ChronoFlow-EmberFlow.git@2f67ab65bc8b9976578fead7ae731664c901994f
fastapi>=0.115
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4
